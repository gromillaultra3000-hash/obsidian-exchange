# API Overview

Lumi exposes health/version/runtime, providers, routing, validation, conflict, policy/actions, history/explainability/dialog, integration, project scanner, patch planner, sandbox, UI, and persistence endpoints.

New v1.2 persistence endpoints are under `/persistence/*`.
