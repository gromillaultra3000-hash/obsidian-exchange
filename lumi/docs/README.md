# Lumi

Embeddable provider orchestration module / decision resolver runtime / integration runtime for host applications.

Current build: v1.2.0 Persistence, Profiles, Local Storage & Hardening Foundation.

Run `python run_lumi.py`, then open `/ui`.
