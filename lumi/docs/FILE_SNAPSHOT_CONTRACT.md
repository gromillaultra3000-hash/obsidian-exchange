# File Snapshot Contract

File snapshots are host-supplied summaries of files. Lumi v0.8 does not read files from disk.

`contentPreview` is optional, truncated to 4000 characters, and redacted. Binary snapshots do not store previews.
