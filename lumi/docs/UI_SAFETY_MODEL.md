# UI Safety Model

The dashboard cannot apply patches, write files, run unrestricted commands, call external URLs, or disable backend guards. It is a local control panel over existing REST contracts.
