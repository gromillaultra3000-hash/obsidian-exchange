# Patch Plan Preview

Patch plan previews describe possible future changes. In v0.8:

- `canApply` is always `false`
- `applyBlockedReason` is `real_file_write_disabled_in_v0_8`
- no diff is produced
- no files are written
