# Apply Preparation Package

An apply preparation package summarizes a planned change and gathers patch, diff, test, and rollback references for review. It does not apply changes to the host project.

`canApplyToHost` is always `false` in v1.0.
