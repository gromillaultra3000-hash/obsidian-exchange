# Command Execution Guard

The guard blocks destructive commands, network commands, install commands, git commands, shell operators, path traversal, absolute path execution, inline interpreters such as `python -c`, and unknown commands.

Unknown commands fail closed.
