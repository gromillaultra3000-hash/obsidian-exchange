# Host App Manifest

A host manifest includes `hostAppId`, `displayName`, `appType`, `allowedModes`, `capabilitiesRequested`, `actionsAllowed`, `eventsSupported`, `callbacks`, and `metadata`.

Never put raw secrets in manifest fields.
