# UI Dashboard

Open `http://127.0.0.1:8000/ui` or `http://127.0.0.1:8000/dashboard` after running `python run_lumi.py`.

The dashboard is served locally by FastAPI and uses vanilla JavaScript. No npm, Vite, React, CDN, or external scripts are required.
