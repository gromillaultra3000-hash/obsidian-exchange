# CONTROLLED REAL APPLY

Lumi v1.7 controlled real apply foundation. Apply is disabled by default and only operates inside registered safe workspaces. The runtime blocks path traversal, secret-like files/content, binary files, delete/rename by default, shell execution, git execution, and network actions. Backup and rollback metadata are required for safe controlled apply flows.
