# Project Manifest

A project manifest identifies a host project and declares allowed scan modes, entry points, test paths, config files, and docs.

Required fields:

- `projectId`
- `displayName`
- `projectType`
- `allowedScanModes`

Invalid manifests are rejected. Metadata is redacted.
