# Зависимости ТЕСТОВ — не боевого пути.
#
# Отдельно от relay/core/requirements.txt намеренно: там перечислено то, без
# чего не работает прод (coincurve), и добавлять туда тестовые библиотеки
# значило бы ставить их на боевые интерпретаторы без нужды.
#
# Ставит их CI:  pip install -r tests/requirements.txt
# Локально всё это уже есть в /root/bot/venv.

# Чужие реализации подписи сообщения. Смысл именно в том, что они НЕ наши:
# свою подпись своим же кодом подтвердит и ошибочный код. eth_account — то же,
# что делает MetaMask personal_sign; tronpy — то же, что TronLink signMessageV2.
eth-account>=0.13,<1
tronpy>=0.6,<1

# Провайдеры и читатели цепей тянут requests на уровне модуля; без него
# половина наборов падает с ModuleNotFoundError и выглядит как настоящая
# поломка кода.
requests>=2.28

# TON Connect: проверка подписи ton_proof (ed25519).
cryptography>=41

# Кошелёк BTC/LTC. Набор `test_btc_wallet.py` стоял в CI и до 06.08.2026, но
# библиотеки в списке установки не было — то есть шаг падал на импорте и ничего
# не проверял. Обнаружено при прогоне CI-списка в чистом окружении.
bitcoinlib>=0.7,<1
