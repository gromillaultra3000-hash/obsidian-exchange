{"schemaVersion": "e4-payment-terminal-pages.v1", "inputs": [{"path": "relay/repositories/payment_status_read_store.py", "sha256": "5cbd26d0335c0cf713aa4be9c87999773bbcc37ab89202a554a4811b6702b6f9"}, {"path": "relay/core/order_access.py", "sha256": "15d996a1450ff898ef62b3e43c048069c2c4dc60863399bcba144cfd46c69a0f"}, {"path": "relay/core/telegram_freshness.py", "sha256": "40bef427a0daa3069e05565fdb65217836414ed581960b64f1cff5c1c266d601"}, {"path": "relay/core/requisites.py", "sha256": "a6c9eb87a236081095eb8213828b4ea33e9296d6f1bb8696d2e91c9733aca507"}, {"path": "relay/services/requisite_origin.py", "sha256": "5552f1c274bc2e7721068bf7db69564ca360d5065920cebbb17c022df4335398"}, {"path": "relay/core/txid.py", "sha256": "726b98d97696578b0883f5a14eca119f9bc4ce0fd92f409617edf1190129f824"}, {"path": "output/manual/e4-payment-terminal-20260908/baseline/relay-fastapi/main.py", "sha256": "1297db5e84e36ddec0cdf09808c871ce55c28cfd7c4ca04bded9bbace5dde2ab"}, {"path": "docs/e4-payment-terminal/acceptance-probe.py", "sha256": "25d8bb3e1dff58024aa19149fcf0f02342d50f9c0d813b7a3430bf742ed90402"}], "pages": [{"name": "expired-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #1 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #1 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 1, \"token\": \"synthetic-latest-1\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #1 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #1</div>\n    <h1>Заявка истекла</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите — создайте новую заявку с актуальным курсом.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #2 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #2 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 2, \"token\": \"synthetic-latest-2\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #2 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #2</div>\n    <h1>Заявка истекла</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите — создайте новую заявку с актуальным курсом.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #3 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #3 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 3, \"token\": \"synthetic-latest-3\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #3 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #3</div>\n    <h1>Заявка истекла</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите — создайте новую заявку с актуальным курсом.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #4 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #4 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 4, \"token\": \"synthetic-latest-4\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #4 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #4</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #5 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #5 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 5, \"token\": \"synthetic-latest-5\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #5 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #5</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #6 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #6 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 6, \"token\": \"synthetic-latest-6\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #6 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #6</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #7 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #7 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 7, \"token\": \"synthetic-latest-7\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #7 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #7</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #8 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #8 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 8, \"token\": \"synthetic-latest-8\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #8 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #8</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "expired-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #9 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #9 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 9, \"token\": \"synthetic-latest-9\", \"status\": \"expired\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-expired-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #9 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #9</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "expired", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #10 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #10 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 10, \"token\": \"synthetic-latest-10\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #10 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #10</div>\n    <h1>Заявка не выполнена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #11 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #11 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 11, \"token\": \"synthetic-latest-11\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #11 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #11</div>\n    <h1>Заявка не выполнена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #12 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #12 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 12, \"token\": \"synthetic-latest-12\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #12 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #12</div>\n    <h1>Заявка не выполнена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #13 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #13 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 13, \"token\": \"synthetic-latest-13\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #13 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #13</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #14 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #14 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 14, \"token\": \"synthetic-latest-14\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #14 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #14</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #15 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #15 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 15, \"token\": \"synthetic-latest-15\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #15 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #15</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #16 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #16 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 16, \"token\": \"synthetic-latest-16\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #16 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #16</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #17 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #17 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 17, \"token\": \"synthetic-latest-17\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #17 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #17</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "failed-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #18 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #18 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 18, \"token\": \"synthetic-latest-18\", \"status\": \"failed\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-failed-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #18 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #18</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "failed", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #19 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #19 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 19, \"token\": \"synthetic-latest-19\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-absent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #19 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #19</div>\n    <h1>Заявка отменена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #20 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #20 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 20, \"token\": \"synthetic-latest-20\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-absent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #20 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #20</div>\n    <h1>Заявка отменена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #21 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #21 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 21, \"token\": \"synthetic-latest-21\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-absent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #21 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #21</div>\n    <h1>Заявка отменена</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Средства не переводите. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #22 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #22 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 22, \"token\": \"synthetic-latest-22\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-stored-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #22 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #22</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #23 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #23 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 23, \"token\": \"synthetic-latest-23\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-stored-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #23 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #23</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #24 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #24 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 24, \"token\": \"synthetic-latest-24\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-stored-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #24 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #24</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "stored", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #25 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #25 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 25, \"token\": \"synthetic-latest-25\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-sent-none", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #25 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #25</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #26 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #26 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 26, \"token\": \"synthetic-latest-26\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"video\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-sent-video", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #26 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #26</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "video", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "cancelled-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #27 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #27 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 27, \"token\": \"synthetic-latest-27\", \"status\": \"cancelled\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2000-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"pdf\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": true}, {"name": "numeric-cancelled-sent-pdf", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #27 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #27</div>\n    <h1>Заявка закрыта, чек у нас</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Повторно не переводите и новую заявку не создавайте. Напишите в поддержку — разберём вручную по вашему чеку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "cancelled", "txid": "", "verification": "pdf", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "numeric": true, "terminal": true}, {"name": "pending-active", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #28 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #28 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 28, \"token\": \"synthetic-latest-28\", \"status\": \"pending\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2099-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "pending", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": false}, {"name": "pending-review", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #29 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #29 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 29, \"token\": \"synthetic-latest-29\", \"status\": \"pending\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2099-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"sent\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "pending", "txid": "", "verification": "", "receipt": "sent", "delayed": false, "dead": false, "tx_url": ""}, "terminal": false}, {"name": "pending-stored-unavailable", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #30 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #30 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 30, \"token\": \"synthetic-latest-30\", \"status\": \"pending\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2099-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"stored\", \"delayed\": false, \"dead\": true};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "pending", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": true, "tx_url": ""}, "terminal": false}, {"name": "numeric-pending-stored-unavailable", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Заявка #30 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; }\n  * { margin:0; padding:0; box-sizing:border-box; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; display:flex; justify-content:center; align-items:center;\n    padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; text-align:center; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:34px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:24px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .num { font-size:13px; color:var(--muted); margin-bottom:6px; }\n  h1 { font-size:21px; font-weight:700; margin-bottom:10px; }\n  .amt { font-size:15px; color:#c4b5fd; margin-bottom:14px; }\n  p { font-size:14px; color:var(--muted); margin-bottom:26px; }\n  .btn { display:block; padding:15px; border-radius:16px; font-size:15px; font-weight:600;\n    text-decoration:none; color:#fff; background:linear-gradient(135deg,var(--purple),var(--purple2)); }\n</style>\n</head>\n<body>\n  <div class=\"card\">\n    <div class=\"brand\"><div class=\"dot\"></div><span>ObsidianExchange</span></div>\n    <div class=\"num\">Заявка #30</div>\n    <h1>Реквизиты недоступны</h1>\n    <div class=\"amt\">2000 ₽</div>\n    <p>Не переводите по прежним реквизитам. Если уже оплатили — не платите повторно и обратитесь в поддержку.</p>\n    <a class=\"btn\" href=\"https://t.me/Obsidian666999bot\">Открыть бота</a>\n  </div>\n</body>\n</html>", "expected": {"status": "pending", "txid": "", "verification": "", "receipt": "stored", "delayed": false, "dead": true, "tx_url": ""}, "numeric": true, "terminal": false}, {"name": "paid", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #31 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #31 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 31, \"token\": \"synthetic-latest-31\", \"status\": \"paid\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2099-01-01T00:00:00Z\", \"txid\": \"\", \"txUrl\": \"\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "paid", "txid": "", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": ""}, "terminal": false}, {"name": "sent", "html": "<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n<meta charset=\"UTF-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">\n<title>Оплата #32 · ObsidianExchange</title>\n<style>\n  :root { --bg:#050507; --card:rgba(255,255,255,.038); --line:rgba(255,255,255,.09);\n           --txt:#f5f5f7; --muted:#8b8b93; --purple:#8b5cf6; --purple2:#a855f7; --ok:#34d399; }\n  * { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }\n  body { font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Roboto,sans-serif;\n    background:radial-gradient(120% 60% at 50% -10%,rgba(139,92,246,.16),transparent 60%),var(--bg);\n    color:var(--txt); min-height:100vh; min-height:100dvh; display:flex; justify-content:center;\n    align-items:center; padding:24px 18px; line-height:1.45; -webkit-font-smoothing:antialiased; }\n  .card { width:100%; max-width:400px; background:var(--card); border:1px solid var(--line);\n    border-radius:28px; padding:30px 24px 26px; backdrop-filter:blur(30px); }\n  .brand { display:flex; align-items:center; gap:8px; justify-content:center; margin-bottom:22px; }\n  .brand .dot { width:9px; height:9px; border-radius:50%; background:linear-gradient(135deg,var(--purple),var(--purple2)); box-shadow:0 0 12px rgba(139,92,246,.7); }\n  .brand span { font-size:14px; font-weight:600; letter-spacing:.3px; color:#e7e7ea; }\n  .pill { display:inline-flex; align-items:center; gap:6px; font-size:12px; font-weight:600;\n    padding:5px 11px; border-radius:100px; margin:0 auto 16px; }\n  .pill.wait { background:rgba(139,92,246,.12); color:#c4b5fd; }\n  .pill.ok { background:rgba(52,211,153,.12); color:var(--ok); }\n  .pill.dim { background:rgba(255,255,255,.06); color:var(--muted); }\n  .pill.exp { background:rgba(248,113,113,.12); color:#fca5a5; }\n  .pdot { width:6px; height:6px; border-radius:50%; background:currentColor; }\n  .pill.wait .pdot { animation:blink 1.4s infinite; }\n  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.25} }\n  .lbl { font-size:13px; color:var(--muted); text-align:center; }\n  .amount { font-size:40px; font-weight:600; letter-spacing:-.5px; text-align:center; margin:2px 0 4px; }\n  .amount .cur { font-size:22px; color:var(--muted); font-weight:500; margin-left:2px; }\n  .hint { font-size:13px; color:var(--muted); text-align:center; margin:14px 0 6px; }\n  .reqs { margin:16px 0 4px; border:1px solid var(--line); border-radius:18px; overflow:hidden; }\n  .row { display:flex; justify-content:space-between; align-items:center; gap:12px; padding:13px 16px; }\n  .row + .row { border-top:1px solid var(--line); }\n  .row .k { font-size:13px; color:var(--muted); flex-shrink:0; }\n  .row .v { font-size:15px; font-weight:500; text-align:right; word-break:break-all; }\n  .row.main { background:rgba(139,92,246,.06); }\n  .row.main .v { font-size:17px; font-weight:600; letter-spacing:.4px; }\n  .cp { appearance:none; border:none; background:transparent; color:var(--purple2); cursor:pointer;\n    font-size:12px; font-weight:600; padding:6px 8px; margin:-6px -6px -6px 4px; border-radius:8px; }\n  .cp:active { background:rgba(139,92,246,.15); }\n  .qr { display:block; width:196px; height:196px; margin:18px auto 6px; background:#fff;\n    border-radius:16px; padding:10px; }\n  .btn { display:block; width:100%; text-align:center; text-decoration:none; margin-top:16px;\n    padding:15px; border-radius:15px; font-size:15px; font-weight:600; color:#fff;\n    background:linear-gradient(135deg,var(--purple),var(--purple2)); transition:opacity .2s,transform .1s; }\n  .btn:active { transform:scale(.985); opacity:.9; }\n  .timer { text-align:center; font-size:13px; color:var(--muted); margin-top:16px; font-variant-numeric:tabular-nums; }\n  .timer b { color:#c4b5fd; font-weight:600; }\n  .foot { text-align:center; font-size:12px; color:var(--muted); margin-top:14px; }\n  .big-ico { font-size:52px; text-align:center; margin:8px 0 6px; }\n  .spin { width:40px; height:40px; margin:14px auto 8px; border:3px solid rgba(139,92,246,.2);\n    border-top-color:var(--purple2); border-radius:50%; animation:sp 1s linear infinite; }\n  @keyframes sp { to { transform:rotate(360deg); } }\n  a.tx { color:var(--purple2); font-size:13px; word-break:break-all; text-decoration:none; }\n</style>\n</head>\n<body>\n  <div class=\"card\" id=\"card\">\n    <div class=\"brand\"><i class=\"dot\"></i><span>ObsidianExchange</span></div>\n    <div id=\"view\"></div>\n    <div class=\"foot\">Заявка #32 · оплата защищена</div>\n  </div>\n<script>\nconst C = {\"orderId\": 32, \"token\": \"synthetic-latest-32\", \"status\": \"sent\", \"amount\": \"2 000\", \"currency\": \"TON\", \"bank\": \"Fixture bank\", \"recipient\": \"Synthetic Fixture\", \"detailVal\": \"+000 SYNTHETIC ONLY\", \"detailLbl\": \"Телефон (СБП)\", \"payLink\": \"\", \"qr\": \"\", \"expiresAt\": \"2099-01-01T00:00:00Z\", \"txid\": \"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\", \"txUrl\": \"https://tonviewer.com/transaction/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\", \"verification\": \"\", \"foreign\": false, \"receipt\": \"\", \"delayed\": false, \"dead\": false};\nconst $ = h => { const d=document.createElement('div'); d.innerHTML=h; return d.firstElementChild; };\nfunction esc(s){ return String(s==null?'':s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])); }\nfunction cp(t,btn){ navigator.clipboard.writeText(t).then(()=>{const o=btn.textContent;btn.textContent='Скопировано';setTimeout(()=>btn.textContent=o,1400);}); }\n\nfunction viewPay(){\n  const rows = [];\n  if (C.bank) rows.push(`<div class=\"row\"><span class=\"k\">Банк</span><span class=\"v\">${esc(C.bank)}</span></div>`);\n  if (C.recipient) rows.push(`<div class=\"row\"><span class=\"k\">Получатель</span><span class=\"v\">${esc(C.recipient)}</span></div>`);\n  if (C.detailVal) rows.push(`<div class=\"row main\"><span class=\"k\">${esc(C.detailLbl)}</span><span class=\"v\">${esc(C.detailVal)}<button class=\"cp\" data-value=\"${esc(C.detailVal)}\" onclick=\"cp(this.dataset.value,this)\">Копировать</button></span></div>`);\n  const reqBlock = rows.length ? `<div class=\"reqs\">${rows.join('')}</div>` : '';\n  const qrBlock = C.qr ? `<img class=\"qr\" src=\"${C.qr}\" alt=\"QR\">` : '';\n  const linkBtn = C.payLink ? `<a class=\"btn\" href=\"${esc(C.payLink)}\" target=\"_blank\" rel=\"noopener\">Перейти к оплате</a>` : '';\n  const amountCopy = C.detailVal ? '' : (C.payLink ? '' : '');\n  return `\n    <div class=\"pill wait\"><i class=\"pdot\"></i>Ожидаем оплату</div>\n    <div class=\"lbl\">К оплате</div>\n    <div class=\"amount\">${C.amount}<span class=\"cur\"> ₽</span>\n      <button class=\"cp\" onclick=\"cp('${C.amount.replace(/\\u202f/g,'')}',this)\">⧉</button></div>\n    <div class=\"hint\">Переведите <b style=\"color:#c4b5fd\">точную сумму</b> по реквизитам${C.payLink?' на странице оплаты':''}</div>\n    ${qrBlock}${reqBlock}${linkBtn}\n    ${C.foreign?`<div style=\"margin-top:12px;padding:11px 13px;border-radius:12px;background:rgba(234,179,8,.07);border:1px solid rgba(234,179,8,.22);text-align:left;font-size:12px;line-height:1.5;color:#d9d2b8\">\n      <b style=\"color:#fde68a\">Реквизиты зарубежного партнёра</b><br>\n      Российских реквизитов сейчас нет в наличии, поэтому платёж идёт через партнёра из СНГ. Перевод проходит как обычный — из вашего банка, в рублях. Ваш банк может запросить подтверждение или взять комиссию за перевод.\n    </div>`:''}\n    ${C.payLink?'':`<div style=\"margin-top:12px;display:grid;gap:5px;text-align:left;font-size:12px;color:#a9a9b3\">\n      <div><b style=\"color:#c4b5fd\">1.</b> Скопируйте сумму и ${C.detailLbl?esc(C.detailLbl).toLowerCase():'реквизиты'}</div>\n      <div><b style=\"color:#c4b5fd\">2.</b> Переведите ровно ${C.amount} ₽ в приложении вашего банка</div>\n      <div><b style=\"color:#c4b5fd\">3.</b> Дождитесь — крипта придёт автоматически, чек не нужен</div>\n    </div>`}\n    <div class=\"timer\" id=\"timer\"></div>\n    <div style=\"margin-top:14px;padding:13px 14px;border-radius:14px;background:rgba(139,92,246,.07);border:1px solid rgba(139,92,246,.18);text-align:left;font-size:12.5px;line-height:1.55;color:#c9c9d3\">\n      <div style=\"color:#e7e7ea;font-weight:600;margin-bottom:6px\">🔒 Оплата под защитой</div>\n      Переводите <b>ровно ${C.amount} ₽</b> на указанные реквизиты — криптовалюта уйдёт на ваш адрес <b>автоматически</b> сразу после зачисления. Реквизиты выданы платёжным партнёром и действительны только для этой заявки.\n      <a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Реквизиты не подходят или есть вопрос? → Поддержка в Telegram</a>\n    </div>\n    <div class=\"foot\" style=\"margin-top:12px\">Оплата подтвердится автоматически · без комиссии банка</div>`;\n}\nfunction viewVerify(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Требуется подтверждение</div>\n    <div class=\"big-ico\">🎥</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Трейдер запросил ${C.verification==='video'?'видео':'PDF-чек'}</div>\n    <div class=\"hint\">Откройте бота и отправьте ${C.verification==='video'?'короткое видео с чеком':'PDF-чек'} — там же указан ID сделки.</div>`;\n}\nfunction viewPaid(){\n  // Обещание «5–15 минут» верно для авто-выплаты и становится ложью на\n  // ручном разборе — а туда уходит большинство заявок. Пока сервер считает\n  // выплату задержанной, говорим то, что есть: деньги у нас, занимается человек.\n  if (C.delayed) return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Выплата задерживается</div>\n    <div class=\"hint\">Ваши деньги получены и подтверждены. Отправку ${esc(C.currency)||'криптовалюты'} задерживает ручная проверка — заявкой уже занимается сотрудник. <b>Повторно платить не нужно.</b> Номер транзакции придёт в бота.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Оплата получена</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Отправляем ${esc(C.currency)||'криптовалюту'}</div>\n    <div class=\"hint\">Обычно занимает 5–15 минут. Страница обновится сама.</div>`;\n}\nfunction viewSent(){\n  const tx = C.txUrl ? `<a class=\"tx\" href=\"${esc(C.txUrl)}\" target=\"_blank\" rel=\"noopener\">🔗 Транзакция в блокчейне</a>`\n                     : (C.txid?`<div class=\"tx\">${esc(C.txid)}</div>`:'');\n  return `<div class=\"pill ok\"><i class=\"pdot\"></i>Выполнено</div>\n    <div class=\"big-ico\">✅</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">${esc(C.currency)} отправлена на ваш адрес</div>\n    <div class=\"hint\">${tx}</div>`;\n}\nfunction viewExpired(){\n  return `<div class=\"pill dim\"><i class=\"pdot\"></i>Время истекло</div>\n    <div class=\"big-ico\">⌛</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Срок оплаты заявки истёк</div>\n    <div class=\"hint\">Курс фиксируется на 15 минут. Создайте новую заявку в боте или на сайте.</div>`;\n}\nconst SUPPORT = '<a href=\"https://t.me/Obsidian666999bot\" style=\"display:inline-block;margin-top:9px;color:#c4b5fd;text-decoration:none;font-weight:600\">Поддержка в Telegram →</a>';\n// Чек ДОШЁЛ до платёжного партнёра: платёж на проверке, реквизиты убираем —\n// по ним заплатят второй раз.\nfunction viewReceipt(){\n  // Сделка у партнёра закрыта — автоматика её уже не подтвердит, решает\n  // человек. Обещать «обычно до 30 минут» здесь значит назвать срок, которого\n  // никто не держит.\n  if (C.dead) return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка статуса</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны, а ваш чек у нас. <b>Повторно не переводите и новую заявку не создавайте.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Чек получен · проверяем</div>\n    <div class=\"spin\"></div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Проверяем ваш платёж</div>\n    <div class=\"hint\">Заявка НЕ отменена и таймер на неё больше не влияет. Как только платёж подтвердится, ${esc(C.currency)||'криптовалюта'} уйдёт на ваш адрес. Обычно до 30 минут; если дольше — напишите в поддержку.\n    ${SUPPORT}</div>`;\n}\n// Заявка ЗАКРЫТА (истекла/отменена), а чек по ней у нас. Обещать выплату\n// нельзя — решение уже принято; молчать о чеке тоже нельзя — деньги клиент,\n// возможно, отдал. Единственный честный выход — к человеку.\nfunction viewReceiptClosed(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Заявка закрыта · чек у нас</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Разбираем вручную</div>\n    <div class=\"hint\">Эта заявка больше не активна, но ваш чек мы получили. <b>Повторно не переводите и новую заявку не создавайте</b> — напишите в поддержку, разберём по чеку.\n    ${SUPPORT}</div>`;\n}\n// Файл у нас, но партнёру НЕ ушёл, а срок реквизитов вышел. Ни «платите по\n// этим реквизитам» (они мертвы), ни «создайте новую» (клиент мог заплатить).\nfunction viewReceiptStored(){\n  return `<div class=\"pill wait\"><i class=\"pdot\"></i>Файл получен · не передан партнёру</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Нужна проверка человеком</div>\n    <div class=\"hint\">Срок этих реквизитов истёк. Ваш файл у нас, но платёжному партнёру он пока не передан — часто это значит, что вместо PDF-чека пришло фото. <b>Если вы уже перевели деньги, ничего не переводите повторно</b> — напишите в поддержку и пришлите PDF-чек из банка.\n    ${SUPPORT}</div>`;\n}\n// Сделка закрыта у провайдера, а заявка ещё жива. Реквизиты показывать нельзя:\n// перевод по ним уйдёт туда, где его уже никто не ждёт.\nfunction viewDead(){\n  return `<div class=\"pill exp\"><i class=\"pdot\"></i>Реквизиты больше не действуют</div>\n    <div class=\"lbl\" style=\"font-size:15px;color:#e7e7ea;text-align:center\">Реквизиты по этой ссылке недоступны</div>\n    <div class=\"hint\">Реквизиты по этой ссылке больше недоступны. <b>Если вы уже перевели деньги — не переводите повторно.</b> Обратитесь в поддержку для проверки статуса.\n    ${SUPPORT}</div>`;\n}\nlet _timer=null, _localExpired=false;\nfunction render(){\n  const v = document.getElementById('view');\n  const closed = (C.status==='expired'||C.status==='failed'||C.status==='cancelled');\n  if (C.status==='sent') v.innerHTML=viewSent();\n  else if (C.status==='paid') v.innerHTML=viewPaid();\n  // Исход, который объявил СЕРВЕР, старше чека: заявку с дошедшим чеком Слой 0\n  // сам не истекает, а раз статус всё же терминальный — это решение человека,\n  // и обещать по нему выплату было бы враньём в другую сторону.\n  else if (closed && C.receipt) v.innerHTML=viewReceiptClosed();\n  else if (C.verification) v.innerHTML=viewVerify();\n  // А вот локальный таймер чеку не указ: 15 минут кончились, решение впереди.\n  else if (C.receipt==='sent') v.innerHTML=viewReceipt();\n  else if (closed) v.innerHTML=viewExpired();\n  // Мёртвая сессия важнее живого таймера: срок ещё идёт, а платить уже некуда.\n  else if (C.dead) v.innerHTML=viewDead();\n  else if (_localExpired && C.receipt) v.innerHTML=viewReceiptStored();\n  else if (_localExpired) v.innerHTML=viewExpired();\n  // 'stored' до истечения срока — реквизиты показываем: клиент мог ещё не платить.\n  else { v.innerHTML=viewPay(); startTimer(); }\n}\nfunction startTimer(){\n  if(!C.expiresAt) return;\n  const end = new Date(C.expiresAt.replace(' ','T')+ (C.expiresAt.includes('Z')?'':'Z')).getTime();\n  const el = document.getElementById('timer');\n  const tick=()=>{\n    if(!el) return;\n    let s = Math.floor((end-Date.now())/1000);\n    // Локальный таймер — догадка, а не приговор: сервер держит заявку живой,\n    // пока не решит иначе (а с чеком — не истекает вовсе). Раньше здесь\n    // писалось C.status='expired', и это же значение глушило опрос ниже —\n    // страница переставала узнавать что бы то ни было.\n    if (s<=0) { _localExpired=true; if(_timer)clearInterval(_timer); render(); return; }\n    const m=String(Math.floor(s/60)).padStart(2,'0'), ss=String(s%60).padStart(2,'0');\n    el.innerHTML = `Реквизиты действительны ещё <b>${m}:${ss}</b>`;\n  };\n  tick(); if(_timer)clearInterval(_timer); _timer=setInterval(tick,1000);\n}\nasync function poll(){\n  // Останавливаемся только на исходе, который подтвердил СЕРВЕР. Пока заявка\n  // жива, а чек ещё в проверке, решение впереди — и узнать его страница может\n  // единственным способом: продолжая спрашивать. Раньше опрос глушил ЛОКАЛЬНЫЙ\n  // таймер, дописывавший C.status='expired' от себя.\n  if (C.status==='sent' || C.status==='expired' || C.status==='failed'\n      || C.status==='cancelled') return;\n  try {\n    const r = await fetch(`/api/order/${C.orderId}?token=${encodeURIComponent(C.token)}`);\n    if (r.ok) { const d=await r.json();\n      if ((d.status && d.status!==C.status) || ((d.verification||'')!==(C.verification||''))\n          || ((d.receipt||'') !== (C.receipt||'')) || (!!d.delayed !== !!C.delayed)\n          || (!!d.dead !== !!C.dead)) {\n        C.delayed = !!d.delayed; C.dead = !!d.dead;\n        C.status=d.status||C.status; C.verification=d.verification||''; C.txid=d.txid||C.txid;\n        // Чек мог прийти уже после загрузки страницы — клиент отправляет его\n        // в боте, а страница остаётся открытой. Значение то же, что у сервера\n        // ('' | 'stored' | 'sent'): 'stored' обещаний не даёт.\n        C.receipt = d.receipt || '';\n        // Ссылку даёт сервер (core.txid). Своя карта здесь знала три монеты, и\n        // для остальных склеивала пустой префикс с хешем — ОТНОСИТЕЛЬНЫЙ href,\n        // то есть 404 на нашем домене вместо доказательства выдачи.\n        C.txUrl = d.tx_url || '';\n        if(_timer)clearInterval(_timer); render(); }\n    }\n  } catch(e) {}\n}\nrender();\nsetInterval(poll, 5000);\n</script>\n</body>\n</html>", "expected": {"status": "sent", "txid": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa", "verification": "", "receipt": "", "delayed": false, "dead": false, "tx_url": "https://tonviewer.com/transaction/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"}, "terminal": false}], "backend": "Exact captured installed SQLite class + unchanged adapter, query_only in-memory synthetic ledger"}
