Continue the owner's explicitly authorized autonomous roadmap delivery in /root.
This run is ONE bounded code-first iteration. The external supervisor starts a
separate iteration only after this one has a verified completion receipt.

Read AGENTS.md and docs/MEGA_PROMPT.md in full for this new session, the compact
current status at the top of PROJECT_MEMORY.md, docs/CURRENT_ROUTE.md, and only
the relevant gate section of docs/ecosystem-master-roadmap.md. Inspect actual Git
and runtime state. The canonical roadmap is authoritative; do not use
docs/loop_prompt.md, obsolete worktree paths or a historical local Next entry.

The owner on 2026-09-06 explicitly enabled transitions across the full E0–E5
canonical roadmap, replacing the earlier E4-only automation restriction.
The owner authorizes serial reversible implementation, proportional tests,
independent reviews, deployment with a known rollback, runtime verification,
scoped commits and pushes. The initial scheduled item is the interrupted E4
browser-review slice; subsequent scheduled stages come from accepted evidence.
Choose the first remaining prerequisite within that scheduled stage,
state Active route: E# / gate criterion / bounded slice, and complete it.
Do not manufacture low-value follow-up work on an already completed vertical.

For another stage, assess the canonical E0–E5 gates against actual evidence.
Return to the earliest unmet gate; genuine forward gate advancement requires
all earlier mandatory gates VERIFIED. A frozen/owner/external blocker stays
open. The owner allows separately labelled bounded KEYLESS_NONPRODUCTION
preparation of a later canonical stage under such a blocker; it never closes
the earlier gate or enables production. Do not enter commercial/new-coin/tooling
tracks outside E0–E5. Do not invent work to keep the loop running.
Follow docs/autonomous-roadmap-transitions.md and return committed transition
evidence for every stage change. The supervisor enforces the scheduled stage.
If its supplied scope is KEYLESS_NONPRODUCTION, all work in this iteration
must be keyless, isolated and nonproduction; no live rollout is permitted.

064A is frozen. Do not create or reuse a 064A request, nonce, decision,
signature, credential or launch; do not start or modify its activation,
064A PostgreSQL authority, watchdog, ceremony, evidence archives or pinned
release. This freeze does not forbid unrelated, already-authorized reversible
database work required by another canonical item. Existing
money, custody, keys, trading and signing gates remain required. General full
access is not authority for a fresh financial action or new credential.
Do not execute real customer money/trades for verification. Never read or print
secret files, private keys, seed phrases, auth cookies or customer datasets.
Do not send email, Telegram or other messages to others without exact existing
authorization; Git push is authorized. Stop for uncertain money outcomes,
missing credentials, destructive/irreversible work or unavailable rollback.

Use only relevant available agents and tools. Obtain both context-aware
acceptance review and an independent diff/security/tool review, with evidence.
Fix findings and failed tests before deployment. Record negative findings
truthfully; do not claim tests, reviews or deployment occurred if unavailable.
An external blocker ends this run, with no automatic retry or design-only loop.

The supervisor owns a shared writer flock and this run is its sole writer.
Do not start another continuation runner, change its files/config/unit/state,
release its lock, restart it, or launch background agents beyond this iteration.
Wait for all subprocesses and subagents before the final receipt. Preserve
unrelated existing untracked files; never use git add -A or git add . in /root.
Commit only intended explicit paths. Do not reset, clean or force-push.

Before finishing, update the compact durable project status, canonical route
and checked-in secret-free evidence. Record exactly one next canonical E0–E5
item. Commit the bounded product implementation, tests, evidence and status.
Return the required JSON receipt. VERIFIED_NEXT requires actual passing tests,
both passing reviews, verified runtime/rehearsal appropriate to this slice,
the full current Git commit, checked-in evidence paths, no concrete blocker,
and a remaining authorized canonical next item. It is a report, not a gate waiver.
If any requirement is unavailable or incomplete, return BLOCKED with the exact
observed blocker and current state; do not guess or retry an uncertain action.
COMPLETE means the entire E0–E5 route is VERIFIED, with a committed assessment
and evidence for all six gates. A completed slice/stage is not COMPLETE when
other canonical work remains. If no authorized bounded work is possible,
return BLOCKED with the precise reason instead of claiming route completion.
