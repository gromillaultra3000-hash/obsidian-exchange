[Service]
# Signing belongs exclusively to obsidian-payout-worker.service. Keep these
# variables explicitly empty even if an older environment source defines them.
Environment=PAYOUT_SEED=
Environment=WALLET_PAYOUT_PASSWORD=
