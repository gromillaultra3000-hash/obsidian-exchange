[package]
name = "obsidian-wallet-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "staticlib", "lib"]
path = "src/lib.rs"

[dependencies]
obsidian-wallet-core = { path = "../wallet-core" }
uniffi = "=0.32.0"

[lints]
workspace = true
